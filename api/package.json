{
  "name": "shopii-api",
  "version": "0.1.0",
  "description": "Shopii API Service - AI-Powered Shopping Assistant",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.dev.ts",
    "dev:full": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "db:generate": "prisma generate",
    "db:generate:local": "prisma generate --schema=prisma/schema.dev.prisma",
    "db:generate:remote": "prisma generate --schema=prisma/schema.prisma",
    "db:push": "prisma db push --schema=prisma/schema.prisma",
    "db:push:local": "prisma db push --schema=prisma/schema.dev.prisma",
    "db:push:remote": "prisma db push --schema=prisma/schema.prisma",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:studio:local": "prisma studio --schema=prisma/schema.dev.prisma",
    "db:studio:remote": "prisma studio --schema=prisma/schema.prisma",
    "db:seed": "tsx prisma/seed.ts",
    "setup": "npm run db:push:local && npm run db:seed",
    "setup:local": "npm run db:generate:local && npm run db:push:local && npm run db:seed",
    "setup:remote": "npm run db:generate:remote && npm run db:push:remote && npm run db:seed",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "prisma": {
    "schema": "prisma/schema.dev.prisma"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.0",
    "@fastify/swagger": "^9.2.0",
    "@fastify/swagger-ui": "^5.1.0",
    "@google/generative-ai": "^0.24.1",
    "@prisma/client": "^5.22.0",
    "@supabase/supabase-js": "^2.46.1",
    "@tavily/core": "^0.7.1",
    "cheerio": "^1.1.2",
    "dotenv": "^17.2.3",
    "fastify": "^5.1.0",
    "ioredis": "^5.4.1",
    "openai": "^6.16.0",
    "stripe": "^17.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.35",
    "@types/node": "^22.9.0",
    "pino-pretty": "^11.2.2",
    "prisma": "^5.22.0",
    "supabase": "^2.72.8",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
